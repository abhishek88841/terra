pipeline
      {
agent any

    stages
        {
         stage('Build')
             {
             steps
               {
                sh 'ls'
		sh 'ls -lrt'
                 sh 'mkdir phhhro1'
		 sh 'uname'
                }
               }
              }
   }
