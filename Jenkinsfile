pipeline
      {
agent any

    stages
        {
         stage('Build')
             {
             steps
               {
                sh 'ls'
                 sh 'mkdir phhhro1'
		 sh 'uname'
                }
               }
              }
   }
